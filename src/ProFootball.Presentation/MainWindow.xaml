<Window x:Class="ProFootball.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ProFootball"
        Height="360"
        Width="560"
        MinHeight="320"
        MinWidth="480">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="24" FontWeight="SemiBold" Text="ProFootball" />
            <TextBlock Margin="0,4,0,0" Opacity="0.85" Text="WPF baseline with PostgreSQL and session placeholder." />
        </StackPanel>

        <Border Grid.Row="2" Padding="12" BorderBrush="#2F3A4A" BorderThickness="1" CornerRadius="8">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Session Status" />
                <TextBlock x:Name="SessionStateText" Margin="0,8,0,0" />
                <Button x:Name="SessionActionButton"
                        Width="160"
                        Margin="0,12,0,0"
                        Padding="8,4"
                        Click="OnSessionActionClick"
                        HorizontalAlignment="Left" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
